apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fib.fullname" . }}-test-connection"
  labels:
    {{- include "fib.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fib.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
